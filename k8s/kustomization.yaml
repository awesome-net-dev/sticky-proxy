apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sticky-proxy

resources:
  - namespace.yaml
  - secret.yaml
  - configmap.yaml
  - rbac.yaml
  - redis.yaml
  - backend.yaml
  - proxy.yaml
  - pdb.yaml
