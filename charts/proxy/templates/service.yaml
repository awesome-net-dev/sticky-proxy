apiVersion: v1
kind: Service
metadata:
  name: {{ include "sticky-proxy.fullname" . }}
  labels:
    {{- include "sticky-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "sticky-proxy.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
